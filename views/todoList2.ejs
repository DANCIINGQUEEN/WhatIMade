<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>memo</title>
    <style>
        @import url(//fonts.googleapis.com/earlyaccess/jejuhallasan.css);

        @font-face {
            font-family: 'KyoboHandwriting2020A';
            src: url('https://cdn.jsdelivr.net/gh/projectnoonnu/noonfonts_2112@1.0/KyoboHandwriting2020A.woff') format('woff');
            font-weight: normal;
            font-style: normal;
        }

        @font-face {
            font-family: 'BMEuljirooraeorae';
            src: url('https://cdn.jsdelivr.net/gh/projectnoonnu/noonfonts_2110@1.0/BMEuljirooraeorae.woff2') format('woff2');
            font-weight: normal;
            font-style: normal;
        }

        @font-face {
            font-family: 'SBAggroB';
            src: url('https://cdn.jsdelivr.net/gh/projectnoonnu/noonfonts_2108@1.1/SBAggroB.woff') format('woff');
            font-weight: normal;
            font-style: normal;
        }

        h1 {
            margin-top: 3rem;
            font-family: 'SBAggroB';
            text-align: center;
            font-size: 100px;
        }

        h6 {
            text-align: center;
        }

        .memo_bar {
            width: 70%
        }
    </style>
</head>

<body>
    <h1>Memo</h1>

    <div class="mx-auto memo_bar input-group mb-3">
        <input type="text" class="form-control" placeholder="Write memo" aria-label="Write memo" onkeyup="enterkey()"
            id="input_memo">

        <button class="btn btn-outline-secondary" type="button" id="button-addon2">Add</button>

    </div>

    <h6 id="delete_msg">삭제하려면 메모를 클릭</h6>

    <ul class="list-group list-group-flush" id='memo'>

    </ul>
    <script>
        window.onload = init
        var memoList=document.getElementById('memo')
        var memoValue = document.getElementById('input_memo')
        var button = document.getElementById('button-addon2')

        function createMemo() {
            if(memoValue.value===''){return;}
            console.log(memoValue);
            const li=document.createElement('li')
            li.appendChild(document.createTextNode(memoValue.value))
            li.classList.add('item')
            memoList.appendChild(li)
            memoValue.value=''
        }
        function init() {
            button.onclick = function () {
                createMemo()
            }
        }

        function setMemo(){
            
        }

        function getMemo() {
            var memoArray = localStorage.getItem('memoArray')
            if (!memoArray) {
                memoArray = []
                localStorage.setItem('memoArray', JSON.stringify(memoArray))
            } else { memoArray = JSON.parse(memoArray) }
            return memoArray
        }
        function addMemoToDom(key, memoObj) {
            var memo = document.getElementById("memo")

        }


        function enterkey() {
            if (window.event.keyCode == 13) {
                // 엔터키가 눌렸을 때
                createMemo()
            }
        }
    </script>
</body>

</html>